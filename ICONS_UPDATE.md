# Замена эмодзи на SVG-иконки

Все эмодзи заменены на профессиональные SVG-иконки из библиотеки `react-icons/hi2` (Heroicons 2).

## 📦 Установлено

```bash
npm install react-icons
```

## 🔄 Замененные иконки

### FolderContainer (корешок папки)
- ❌ Было: `📁` (эмодзи)
- ✅ Стало: `<HiFolder />` (SVG-иконка)

### Header (логотип)
- ❌ Было: `📁` (эмодзи)
- ✅ Стало: `<HiFolder />` (SVG-иконка)

### Footer (контакты)
- ❌ Было: `📞` `✉️` `💬` (эмодзи)
- ✅ Стало:
  - `<HiPhone />` - телефон
  - `<HiEnvelope />` - email
  - `<HiChatBubbleLeftRight />` - Telegram

### Footer (логотип внизу)
- ❌ Было: `📁` (эмодзи)
- ✅ Стало: `<HiFolder />` (SVG-иконка)

### AboutNew (бейджи квалификаций)
- ❌ Было: `🎓` `⚖️` `📊` (эмодзи)
- ✅ Стало:
  - `<HiAcademicCap />` - МГЮА
  - `<HiScale />` - Медиатор
  - `<HiChartBar />` - Проектное управление

### BusinessNew (примеры кейсов)
- ❌ Было: `💼` (эмодзи)
- ✅ Стало: `<HiBriefcase />` (SVG-иконка)

## 🎨 Стилизация иконок

### Добавленные CSS-правила

**FolderContainer.css**:
```css
.spine-icon {
  font-size: 28px;
  color: var(--text-muted);
  opacity: 0.7;
}
```

**Header.css**:
```css
.logo-icon {
  font-size: 24px;
  color: var(--accent-orange);
}
```

**Footer.css**:
```css
.contact-icon {
  font-size: 20px;
  width: 28px;
  color: var(--accent-orange);
  flex-shrink: 0;
}

.footer-logo-icon {
  font-size: 20px;
  color: var(--accent-orange);
}
```

**AboutNew.css**:
```css
.badge-icon {
  font-size: 24px;
  color: var(--accent-orange);
}

.badge-icon svg {
  width: 24px;
  height: 24px;
}
```

**BusinessNew.css**:
```css
.example-icon {
  font-size: 20px;
  color: var(--accent-orange);
}

.example-icon svg {
  width: 20px;
  height: 20px;
}
```

## ✨ Преимущества замены

### 1. Профессиональный вид
- SVG-иконки четкие на любых экранах (включая Retina)
- Единый стиль всех иконок
- Нет проблем с рендерингом эмодзи в разных браузерах

### 2. Управляемость
- Можно менять цвет через CSS (`color`)
- Легко масштабировать (`font-size` или `width/height`)
- Анимируемые

### 3. Консистентность
- Все иконки из одного семейства (Heroicons 2)
- Одинаковая толщина линий
- Сбалансированные пропорции

### 4. Производительность
- SVG весит меньше, чем эмодзи-шрифты
- Кешируются браузером
- Оптимизированы для веб-использования

## 📚 Используемые иконки из react-icons/hi2

| Компонент | Иконка | Назначение |
|-----------|--------|------------|
| `<HiFolder />` | Папка | Логотип, корешок |
| `<HiPhone />` | Телефон | Контакты |
| `<HiEnvelope />` | Конверт | Email |
| `<HiChatBubbleLeftRight />` | Чат | Telegram |
| `<HiAcademicCap />` | Академическая шапочка | Образование (МГЮА) |
| `<HiScale />` | Весы | Медиатор |
| `<HiChartBar />` | График | Проектное управление |
| `<HiBriefcase />` | Портфель | Бизнес-кейсы |

## 🎯 Цветовая схема иконок

Все иконки используют цвета из палитры:

- **Основные иконки** (логотип, контакты): `var(--accent-orange)` (#DB7A36)
- **Корешок папки**: `var(--text-muted)` (#706555) с opacity 0.7
- **Бейджи и примеры**: `var(--accent-orange)` (#DB7A36)

## 🔧 Технические детали

### Импорт иконок
```javascript
import { HiFolder, HiPhone, HiEnvelope } from 'react-icons/hi2';
```

### Использование в компонентах
```jsx
// Простое использование
<HiFolder className="icon-class" />

// С динамическим выбором (AboutNew)
const IconComponent = credential.icon;
<IconComponent />
```

### Размеры
- **Корешок**: 28px
- **Логотип header**: 24px
- **Бейджи**: 24px (в контейнере 48x48px)
- **Контакты**: 20px
- **Примеры**: 20px

## ✅ Результат

- ✅ Все эмодзи заменены на SVG
- ✅ Иконки окрашены в фирменные цвета
- ✅ Консистентный дизайн
- ✅ Четкое отображение на всех экранах
- ✅ Никаких проблем с кроссбраузерностью

## 🚀 Запуск

Сервер запущен на **http://localhost:3001**

Все иконки отображаются корректно!
