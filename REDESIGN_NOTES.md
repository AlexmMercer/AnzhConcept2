# Заметки по редизайну - Устранение "офисной бежевости"

## 🎯 Основные проблемы старой версии (устранены)

### 1. ❌ Папка не была каркасом
**Было**: Белые карточки на бежевом фоне без связи друг с другом
**Стало**:
- ✅ Трёхслойная структура: стол (#F7F3EC) → папка (#FFFCF9) → листы (белые)
- ✅ Корешок папки идёт вдоль всей высоты страницы (64px, sticky)
- ✅ Все листы лежат внутри одной папки-контейнера
- ✅ Листы имеют эффект стопки (2 полупрозрачных слоя сзади)

### 2. ❌ Вайб "офис 90-е/нулевые"
**Было**: Тяжёлые тени, плотные цвета, клипарт-штампы
**Стало**:
- ✅ Мягкая цветовая палитра:
  - Orange: #DB7A36 (было #D8742A)
  - Purple: #7E45C0 (было #7A3CB8)
  - Градиенты вместо сплошных заливок
- ✅ Лёгкие тени: `0 8px 16px rgba(0, 0, 0, 0.04)` вместо `0.08`
- ✅ Современные бейджи вместо штампов с рамками

### 3. ❌ Нет глубины и структуры досье
**Было**: Плоские секции без иерархии
**Стало**:
- ✅ Слоистая структура листов (translateY + opacity)
- ✅ Эффект перелистывания при скролле (rotateX -2deg → 0)
- ✅ Hover поднимает лист на -2px с усиленной тенью

## 🎨 Новая цветовая система

### Слой 1: Стол (фон сайта)
```css
--bg-desk: #F7F3EC
```
- Тонкий радиальный градиент для текстуры
- Чище и светлее старого #FAF6EF

### Слой 2: Папка
```css
--bg-folder: #FFFCF9
--folder-spine: #E7E0D4
--folder-shadow: 0 20px 40px rgba(0, 0, 0, 0.06)
```
- Почти белая, но тёплая
- Корешок с градиентом

### Слой 3: Листы
```css
--bg-sheet: #FFFFFF
--sheet-border: #E4DDCF
--sheet-shadow: 0 8px 16px rgba(0, 0, 0, 0.04)
```
- Чистый белый
- Тонкие светлые границы

### Акценты
```css
--accent-orange: #DB7A36
--accent-orange-light: #F7C299
--accent-purple: #7E45C0
--accent-purple-light: #E4D5FF
```
- Более мягкие, с градиентами
- Светлые версии для фонов

### Текст
```css
--text-main: #2B1E12 (было #333333)
--text-muted: #706555 (было #777777)
```
- Теплее, коричневатый оттенок
- Меньше серости

## 🏗️ Архитектура компонентов

### Новые компоненты
1. **FolderContainer** - обёртка-папка с корешком
2. **StickyTabs** - прилипающие вкладки с Intersection Observer
3. **SheetContainer** - обёртка для листов с эффектом стопки
4. **HeroNew** - упрощённый Hero без лишних визуалов
5. **AboutNew** - современные бейджи вместо штампов
6. **BusinessNew** - чистые карточки без офисных рамок
7. **CourseNew** - обновлённые стили

### Структура App
```
<Header />
<FolderContainer> ← папка с корешком
  <HeroNew />
  <StickyTabs /> ← навигация с подсветкой

  <SheetContainer id="about"> ← лист со стопкой
    <AboutNew />
  </SheetContainer>

  <SheetContainer id="business">
    <BusinessNew />
  </SheetContainer>

  <SheetContainer id="course">
    <CourseNew />
  </SheetContainer>
</FolderContainer>
<Footer />
```

## ✨ Ключевые улучшения

### Корешок папки (FolderContainer)
- ✅ 64px ширина, sticky позиционирование
- ✅ Вертикальный текст "ВоВсемПрав.ру"
- ✅ Градиентный фон
- ✅ Идёт от начала до конца папки

### Sticky-вкладки (StickyTabs)
- ✅ Фиксируются при скролле (top: 80px)
- ✅ Автоматическая подсветка активной секции (Intersection Observer)
- ✅ Плавный скролл с offset -100px
- ✅ Три состояния: normal → hover → active
- ✅ Градиенты для активной вкладки

### Листы со стопкой (SheetContainer)
- ✅ 2 полупрозрачных слоя сзади (opacity 0.3 и 0.5)
- ✅ Смещение: translateY(4px/8px) + translateX(3px/6px)
- ✅ Анимация появления: translateY(20px) + rotateX(-2deg) → 0
- ✅ Hover: поднятие на -2px + усиление тени

### Штампы → Бейджи (AboutNew)
**Было**: Рамки, уголки, клипарт
**Стало**:
- Иконка в белом квадрате с тенью
- Градиентный фон бейджа
- Hover: сдвиг вправо + усиление фона
- Без офисных рамок

### Карточки услуг (BusinessNew)
**Было**: Тяжёлые рамки 2px, таблица CRM
**Стало**:
- Белый фон с 1px прозрачной рамкой
- Hover: градиентный фон + тонкая рамка
- Левая оранжевая линия 3px при hover
- Scale 1.01 вместо translateX
- Плавный cubic-bezier(0.4, 0, 0.2, 1)

### Формы
- ✅ Floating labels с cubic-bezier анимацией
- ✅ 2px подчёркивание вместо рамки
- ✅ Градиентный фон карточек форм
- ✅ Кнопки с градиентами и inset бликом при hover

## 🎬 Анимации

### Загрузка Hero
```css
animation: heroFadeIn 0.8s cubic-bezier(0.4, 0, 0.2, 1);
```
- Плавное появление снизу вверх

### Появление листов
```css
transform: translateY(20px) rotateX(-2deg);
transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
```
- Эффект "перелистывания"

### Последовательное появление тем курса
```javascript
topics.forEach((_, index) => {
  setTimeout(() => setVisibleTopics(...), index * 120);
});
```

### Hover эффекты
- Кнопки: translateY(-3px) + тень
- Карточки: scale(1.01) + градиентный фон
- Листы: translateY(-2px) + усиление тени

## 📱 Адаптивность

### Desktop (≥1024px)
- Корешок 64px
- Все эффекты стопки активны
- Sticky-вкладки горизонтально

### Tablet (768-1023px)
- Корешок сохраняется
- Колонки превращаются в вертикальный стек
- Sticky-вкладки остаются горизонтальными

### Mobile (≤767px)
- Корешок 48px
- Эффект стопки листов отключен
- Sticky-вкладки вертикально, во всю ширину
- Tooltips фиксируются по центру экрана

## 🔧 Технические детали

### Intersection Observer для вкладок
```javascript
rootMargin: '-100px 0px -60% 0px'
threshold: 0
```
- Активируется когда секция в верхней части viewport

### CSS-переменные для смещения листов
```jsx
<SheetContainer offset={4}>
  style={{ '--offset': `${offset}px` }}
```

### Градиенты кнопок
```css
background: linear-gradient(135deg, var(--accent-orange), #E4863C);
box-shadow: 0 8px 20px rgba(219, 122, 54, 0.2);
```

### Cubic Bezier timing
```css
transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
```
- Более плавная, чем ease/ease-out

## 📊 Сравнение: Было → Стало

| Аспект | Было | Стало |
|--------|------|-------|
| Структура | Плоские карточки | Папка → Листы → Контент |
| Навигация | Закладки только в Hero | Sticky-вкладки с auto-highlight |
| Цвета | Плотные, офисные | Мягкие градиенты |
| Тени | Тяжёлые (0.08) | Лёгкие (0.04) |
| Штампы | Рамки + уголки | Современные бейджи |
| Карточки | Таблица CRM | Воздушные карточки |
| Анимации | Простые fade | Rotate + translate + scale |
| Корешок | Нет | Sticky 64px на всю высоту |

## 🎯 Что достигнуто

### ✅ Папка = каркас
- Видна с первой секунды
- Объединяет весь контент
- Корешок создаёт ось координат

### ✅ Современный визуал
- Убрана офисная бежевость
- Мягкие цвета и градиенты
- Лёгкие тени
- Воздушная типографика

### ✅ Физика досье
- Листы лежат стопкой
- Эффект перелистывания
- Глубина и иерархия

### ✅ Sticky-навигация
- Вкладки всегда доступны
- Автоподсветка активной секции
- Плавные переходы

## 🚀 Готовность

Новая версия полностью функциональна:
- ✅ Все компоненты работают
- ✅ Формы валидируются
- ✅ Анимации плавные
- ✅ Адаптив на всех устройствах
- ✅ Intersection Observer для вкладок
- ✅ Все цвета обновлены

## 📝 Что осталось от старой версии

- Header (без изменений)
- Footer (без изменений)
- Логика форм (обновлены только стили)
- Базовая структура контента

## 🎨 Финальная цветовая палитра

```css
Фоны:
  Стол: #F7F3EC
  Папка: #FFFCF9
  Листы: #FFFFFF

Акценты:
  Orange: #DB7A36 → #E4863C (gradient)
  Purple: #7E45C0 → #8B52CF (gradient)

Текст:
  Main: #2B1E12
  Muted: #706555

Границы:
  Light: #E4DDCF
  Spine: #E7E0D4
```

---

## 🎉 Итог

Сайт теперь выглядит как современный legal-tech продукт, а не как SharePoint из нулевых. Папка действительно является каркасом, листы имеют физику, а вкладки работают как полноценная навигационная система.
